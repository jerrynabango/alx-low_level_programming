#include <stdio.h>
/**
 * main - A program that prints the name of the file it was compiled
 *
 * Return: Displays the name of the file compiled
 */
int main(void)
{
	printf("%s\n",__FILE__);

	return (0);
}
