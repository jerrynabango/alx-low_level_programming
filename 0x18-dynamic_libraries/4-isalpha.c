#include "main.h"
/**
 * _isalpha - A function that checks if charcater is alphabet
 *
 * @c: Indicates the character
 *
 * Return: Displays alphabet characters
 */
int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}

