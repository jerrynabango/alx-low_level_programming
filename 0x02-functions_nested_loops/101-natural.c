#include <stdio.h>
/**
 * main -  computes sum of all the multiples of 3 or 5 below 1024
 *
 * Return: Indicates successful execution
 */
int main(void)
{
	int j, z;

	for (j = 1 ; a < 1024 ; a++)
	{
		if ((j % 3) == 0 || (j % 5) ==)
			z += j;
	}
	printf("%d\n", z);
	return (0);
i}
